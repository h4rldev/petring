# You will need a .env for this docker-compose file

# Deployment :
# uncomment the entire watchtower section
# uncomment traefik if needed

services:
  petring-web:
    container_name: petring-web
    build: ./web # <- Comment this out and uncomment the thing below if you're not cloning the repo
    # image: ghcr.io/h4rldev/petring-web:latest
    labels:
      com.centurylinklabs.watchtower.scope: petring-web

    ports: # Unneeded if you're using traefik
      - 8080:8080

    # networks: # For traefik (make sure the name is the same as your traefik network)
    #   - proxy

    volumes:
      - ./petring-web.toml:/app/petring-web.toml
      - ./frontend/:/app/frontend/

  petring-api:
    container_name: petring-api
    build: ./api # <- Comment this out and uncomment the thing below if you're not cloning the repo
    # image: ghcr.io/h4rldev/petring-api:latest
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ACCESS_TOKEN_SECRET=${ACCESS_TOKEN_SECRET}
      - REFRESH_TOKEN_SECRET=${REFRESH_TOKEN_SECRET}
      - BOT_TOKEN=${BOT_TOKEN}
    labels:
      com.centurylinklabs.watchtower.scope: petring-api

    # networks: # For traefik (make sure the name is the same as your traefik network)
    #   - proxy

    ports: # Unneeded if you're using traefik
      - 8081:8081

    volumes:
      - ./petring-api.toml:/app/petring-api.toml
      - ./petring.db:/app/petring.db
#   watchtower-web: # For auto-updates of petring-web
#     image: containrrr/watchtower
#     container_name: watchtower_petring-web
#     volumes:
#       - "/var/run/docker.sock:/var/run/docker.sock"
#     command: --interval 30 --scope petring-web
#     labels:
#       com.centurylinklabs.watchtower.scope: petring-web

#   watchtower-api: # For auto-updates of petring-api
#     image: containrrr/watchtower
#     container_name: watchtower_petring-api
#     volumes:
#       - "/var/run/docker.sock:/var/run/docker.sock"
#     command: --interval 30 --scope petring-api
#     labels:
#      com.centurylinklabs.watchtower.scope: petring-api

# networks: # For use with traefik (change it so it matches your traefik network)
#   proxy:
#     name: proxy # This and traefik.docker.network must match
#     external: true
